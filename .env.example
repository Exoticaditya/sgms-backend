# ========================================
# SGMS BACKEND ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env and fill in actual values
# NEVER commit .env to version control!

# ========================================
# DATABASE CONFIGURATION (REQUIRED)
# ========================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# 
# Local Development:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sgms
#
# Railway Production (auto-set by Railway):
# DATABASE_URL=postgresql://postgres:xxx@host.railway.app:5432/railway
DATABASE_URL=postgresql://user:password@host:port/database

# ========================================
# JWT SECURITY (REQUIRED)
# ========================================
# Secret key for JWT token signing
# MUST be at least 32 characters long
# Generate using: openssl rand -base64 32
APP_SECURITY_JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_required

# Token expiration (optional, defaults shown)
APP_SECURITY_JWT_ACCESS_TOKEN_TTL_SECONDS=86400
APP_SECURITY_JWT_ISSUER=sgms-backend

# ========================================
# CORS CONFIGURATION (REQUIRED)
# ========================================
# Comma-separated list of allowed frontend origins
# Development:
APP_CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
#
# Production:
# APP_CORS_ALLOWED_ORIGINS=https://your-frontend.netlify.app,https://zplusesecurity.com

# ========================================
# SERVER CONFIGURATION (OPTIONAL)
# ========================================
# Server port (defaults to 8080)
SERVER_PORT=8080

# Active Spring profile (local, dev, prod)
SPRING_PROFILES_ACTIVE=prod

# ========================================
# LOGGING (OPTIONAL)
# ========================================
LOGGING_LEVEL_COM_SGMS=INFO
LOGGING_LEVEL_ORG_SPRINGFRAMEWORK=WARN

# ========================================
# FLYWAY DATABASE MIGRATIONS (OPTIONAL)
# ========================================
# Migrations run automatically on startup
# Disable migrations (not recommended):
# SPRING_FLYWAY_ENABLED=false

# ========================================
# ACTUATOR (OPTIONAL)
# ========================================
# Health check endpoint configuration
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info
MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS=when-authorized

# ========================================
# IMPORTANT SECURITY NOTES
# ========================================
# 1. NEVER commit .env file to git (already in .gitignore)
# 2. Use different JWT secrets for dev/staging/production
# 3. Rotate JWT secret regularly (requires users to re-login)
# 4. Keep database credentials secure
# 5. Use strong passwords (minimum 12 characters)
# 6. Enable SSL/TLS in production
# 7. Whitelist only trusted CORS origins
